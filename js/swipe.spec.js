'use strict';

describe('myApp.swipe module', function() {

  beforeEach(module('myApp.swipe'));

  describe('swipe controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var SwipeCtrl = $controller('SwipeCtrl');
      expect(SwipeCtrl).toBeDefined();
    }));

  });
});